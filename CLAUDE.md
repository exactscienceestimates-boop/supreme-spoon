# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This is a minimal test repository (`supreme-spoon`) with no application code, build system, or tooling configured yet. The only file beyond standard git metadata is a placeholder `README.md`.

There are currently no build, lint, or test commands to run — these should be set up as the project grows.
